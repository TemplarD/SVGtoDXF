# Журнал разработки SVG to DXF Converter

## 🐾 2026-08-25 - ИСПРАВЛЕНИЕ НЕДОЧЁТОВ И БАГОВ (UX + Windows)

### ✅ Выполнено:
1. ✅ **Анимация-подсказка кнопки «Конвертировать»** — пока не выбраны SVG
   и/или папка вывода, кнопка пульсирует (`needs-action`) и рядом появляется
   подсказка-стрелочка с действием, которое нужно сделать.
2. ✅ **Чекбокс «Заменять файлы»** рядом с кнопкой конвертации. Без него
   (по умолчанию) при совпадении имён создаются файлы с индексом `_1`, `_2`, …
   вместо перезаписи.
3. ✅ **Суффиксы имени `_color` / `_hatch`** — активные чекбоксы в группе
   «Имя файла», доступные только при включённых «Сохранять цвета» /
   «Заливка линиями» соответственно.
4. ✅ **Группировка чекбоксов** — разделены на логичные блоки «Качество»
   (заливка, цвета, точные цвета, трассировка растра) и «Имя файла»
   (суффиксы + пояснение про индексы).
5. ✅ **Логика уникальных имён в backend** (`api_convert_files`) — честная
   генерация индексов при `overwrite=false`, без перезаписи результатов
   даже при нескольких файлах с одинаковым базовым именем в одном прогоне.
6. ✅ **Windows: SmartScreen** — в `tauri.conf.json` добавлен
   `bundle.windows.timestampUrl` (Digicert) + `digestAlgorithm=sha256` и
   NSIS-установка `currentUser`; расписано, как применить подпись
   Authenticode в `build/windows.ps1`.
7. ✅ **Windows: убрано консольное окно** — `.cargo/config.toml` с
   `windows_subsystem="windows"` для `x86_64-pc-windows-msvc`; `println!`
   в `setup` заменён на `tracing::info!`.

### 🐛 Исправлено:
- Консольное окно при запуске `.exe` (закрытие которого завершало приложение).
- Отсутствие защиты от перезаписи выходных DXF.
- Недоступность суффиксов имени при выборе цвета/штриховки.

---

## 🚀 2026-01-30 02:23 - ПОЛНАЯ РЕСТРУКТУРИЗАЦИЯ ПО ТЗ v2.0.0

### ✅ Выполнено:
1. ✅ **Создана модульная архитектура** - workspace с 4 модулями согласно ТЗ
2. ✅ **Реализован core модуль** - конвертация SVG в DXF с usvg
3. ✅ **Создан UI модуль на Yew** - современный фронтенд на Rust
4. ✅ **Построен tauri_app модуль** - Tauri 2.0 интеграция
5. ✅ **Разработан integration_tests** - система автотестирования
6. ✅ **Внедрена система tracing** - централизованное логирование
7. ✅ **Добавлена дебаг-панель F12** - отладка и тестирование в UI

### 🏗️ Новая структура:
```
crates/
├── core/           # Ядро конвертации (usvg + dxf)
├── ui/             # Frontend на Yew
├── tauri_app/      # Tauri интеграция
└── integration_tests/  # Автотесты
```

### 📦 Обновлены зависимости:
- ✅ `usvg` для парсинга SVG (вместо svg)
- ✅ `tracing` для логирования (вместо log)
- ✅ `yew` для фронтенда (вместо HTML/JS)
- ✅ `tauri 2.0` с плагинами
- ✅ `serde-wasm-bindgen` для WebAssembly

---

## 📝 ИСТОРИЯ РАЗРАБОТКИ (v1.0.0)

**Предыдущая версия создана на HTML/JS + Tauri:**

### 🔄 Этапы разработки:
- **2026-01-28 15:15** - Исправление проблем диалогов, добавлена домашняя директория
- **2026-01-29 19:13** - UI исправления, добавлена F12 дебаг-панель, интеграция автотестов  
- **2026-01-29 20:17** - Улучшение навигации, обработка пустых директорий, логирование
- **2026-01-29 21:50** - Критические исправления загрузки файлов, автотест расширен
- **2026-01-29 21:20** - Финальное завершение: демо-режим, Tauri интеграция, 100% тесты

### 🎯 Итог v1.0.0:
- ✅ Полнофункциональный браузерный демо-режим
- ✅ Tauri интеграция с файловой системой
- ✅ Автотест: 3/3 тестов (100% успех)
- ✅ Базовая конвертация SVG→DXF

---
